Webpack starter kit using ES6, Babel, SASS, Autoprefixer, auto-reload and source-maps.

`npm run build`
Generates the bundle

`npm start`
Generates the bundle, runs on localhost:8080 and listens for changes.
